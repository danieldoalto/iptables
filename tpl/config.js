// Arquivo de configuração da aplicação

const appConfig = {
    // Defina como 'true' para usar caminhos absolutos para os assets (CSS, JS).
    // Útil para ambientes com proxy ou roteamento complexo.
    use_absolute_paths: true,

    // O caminho base para os assets se 'use_absolute_paths' for true.
    // Para um servidor na raiz, o valor é '/'.
    asset_path: '/'
};
